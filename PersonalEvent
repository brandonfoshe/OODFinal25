public class PersonalEvent extends Event {

    //HEADER:
    //Personal event class, only needs to call the super for the constructor

    public PersonalEvent(int time, int duration, String title, String description, String location, int day) {
        super(time, duration, title, description, location, day);
    }

}
