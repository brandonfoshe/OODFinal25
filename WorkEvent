public class WorkEvent extends Event {
    //HEADER:
    //Work event class, calls the super for constructor, different type than PersonalEvent

    public WorkEvent(int time, int duration, String title, String description, String location, int day) {
        super(time, duration, title, description, location, day);
    }
}
